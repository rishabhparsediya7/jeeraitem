/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com"],
    formats: ["image/gif", "image/jpg", "image/svg", "image/webp"],
  },
};

export default nextConfig;
